package com.example.ui.theme

import androidx.compose.ui.graphics.Brush
import androidx.compose.ui.graphics.Color

object AftermaGradients {
    val PrimaryHero = Brush.linearGradient(
        colors = listOf(
            Color(0xFFFF5FA2),
            Color(0xFFE94B93),
            Color(0xFFC93C78)
        )
    )

    val SoftWellness = Brush.linearGradient(
        colors = listOf(
            Color(0xFFFFF1F6),
            Color(0xFFFFE5F0)
        )
    )

    val Healing = Brush.linearGradient(
        colors = listOf(
            Color(0xFFD8F0FF),
            Color(0xFFEADCF8)
        )
    )

    val CalmGreen = Brush.linearGradient(
        colors = listOf(
            Color(0xFFEAFBF3),
            Color(0xFFB8F2D6)
        )
    )

    val WarmPeachGradient = Brush.linearGradient(
        colors = listOf(
            Color(0xFFFFF4EC),
            Color(0xFFFFD7C2)
        )
    )
}
